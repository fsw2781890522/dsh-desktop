/**
 * The retired web-react package had no host-side service. Keep its namespace
 * loadable as a Cordis entry while its compatibility surface lives in
 * `./client` and is delivered through the browser module table.
 */
export function apply() {}
