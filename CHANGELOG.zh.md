# 更新日志

桌面壳的历次发布记录写在这里。运行中的应用从 `releases/latest.json` 读取同一份说明。

## 0.3.0 — 2026-08-18

- 运行时基线换成官方 `@deepseek-ai/dsh@0.1.0-rc.7`，并保留桌面侧成果：无边框 Tauri 壳、可调透明度的侧栏毛玻璃、代理端口回退、默认模型、Anchored Standard、prompt 导航、「已处理」过程折叠，以及去掉设置按钮上方的 session 列表底部渐变。
- 「检查更新」、本地 HTTP 代理与智能体 web 工具仍分属不同平面。用户数据仍在 `~/.dsh`。
- 首次启动完整性检查接受 npm 提升到 `@deepseek-ai/` 旁的 `commander`，不再只认 dsh 包内的嵌套副本。
- 窗口失焦后仍保留原生 Acrylic：壳会重刷持久 composition 属性，而不是依赖 Windows 11 在失活窗口上会停画的 transient 背景。
- 设置里删除提供方或预设的确认框会显示在设置面板之上。

## 0.2.3 — 2026-08-17

- 选择 session 后一次性加载完整历史，聊天视图取消容易误触的「加载更早」控件。
- prompt 导航收录 agent 推理过程中的 steering/插话；侧边栏改为浅色/深色主题下 60% 语义底色、40% 背景采样的可读毛玻璃效果。
- 设置面板保持最高层级的全窗口显示，不再被输入框、prompt 导航或侧栏玻璃层遮挡；历史加载提示移至 session 中部并适当放大。
- 出厂 `Anchored Standard` 预设仍作为新建 session 默认模式，但归入「自定义」项。
- 移除 session 列表底部遮挡内容的渐变边界；原生 Acrylic 在失焦后自动恢复，侧栏玻璃继续适配浅色/深色主题。
- 通用设置新增侧栏透明度滑块；底部统计预览和普通 Tooltip 改用主题感知的浮层表面，浅色主题不再显示深色预览框。
- 启动页保留靠下的运行时解压进度提示，便于解释重装后首次启动较慢的原因。
- 进程 HTTP 直连超时只约束响应头到达；不再在约 5 秒时误杀仍在传输的模型 SSE，从而避免官方 API 可达时反复出现「已重试模型请求」。

## 0.2.2 — 2026-08-16

- 首次启动改为解到临时目录，先结束残留的 `bundle-runtime/node.exe`，再替换运行时目录，避免 Windows `tar` 被孤儿 Node 进程挡住。

## 0.2.1 — 2026-08-16

- 「检查更新」与 harness 使用同一套直连失败再走本地代理的规则（`127.0.0.1:{port}`，默认 7897）。这不会启用 agent 的 web 工具。

## 0.2.0 — 2026-08-16

- 设置里可以检查更新通道，并把更新的 NSIS 安装包装回当前安装目录。
- 安装包拷到 `releases/<version>/`，不再覆盖单一 setup.exe。
- 用户数据仍在 `~/.dsh`。
