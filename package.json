{
  "name": "dsh-desktop",
  "version": "0.3.2",
  "private": true,
  "description": "DeepSeek Harness as a Windows desktop app: Tauri shell that bundles Node.js + the official dsh CLI and serves the Web UI on 127.0.0.1.",
  "scripts": {
    "icon": "tauri icon icon-source/deepseek-black-1024.png",
    "icon:render": "node scripts/render-icon.mjs",
    "tauri": "tauri",
    "build": "tauri build",
    "dev": "tauri dev"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2",
    "@tauri-apps/cli": "^2"
  }
}
